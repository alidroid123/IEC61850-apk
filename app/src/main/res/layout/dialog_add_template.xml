<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="300dp"
    android:layout_height="wrap_content"
    android:layout_gravity="center"
    app:cardCornerRadius="@dimen/card_corner_radius_huge"
    app:cardBackgroundColor="?attr/customSurfaceColor"
    app:strokeColor="?attr/colorPrimary"
    app:strokeWidth="1dp"
    app:cardElevation="@dimen/card_elevation_huge">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="@dimen/spacing_l">

        <TextView
            android:id="@+id/tvAddTemplateDialogTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="@string/ttl_tmpl_add_dialog"
            android:textColor="?attr/colorPrimary"
            android:textSize="@dimen/text_size_body_large"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_marginBottom="@dimen/spacing_m" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:background="@color/divider"
            android:layout_marginBottom="@dimen/spacing_m"
            android:alpha="0.3" />

        <EditText
            android:id="@+id/etTemplateName"
            style="@style/SettingsInput"
            android:layout_marginTop="0dp"
            android:inputType="text"
            android:hint="@string/hint_tmpl_name" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="@dimen/spacing_l"
            android:gravity="end">

            <Button
                android:id="@+id/btnCancelAddTemplate"
                style="@style/Widget.MaterialComponents.Button.TextButton"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/btn_all_cancel"
                android:textColor="@color/text_secondary" />

            <Button
                android:id="@+id/btnConfirmAddTemplate"
                style="@style/Widget.DFRtools.Button"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="@dimen/spacing_s"
                android:text="@string/btn_all_add"
                android:paddingHorizontal="@dimen/spacing_l" />
        </LinearLayout>

    </LinearLayout>
</com.google.android.material.card.MaterialCardView>
